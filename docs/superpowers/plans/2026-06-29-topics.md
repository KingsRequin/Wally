# Topics — Sujets de communauté enrichis — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Faire évoluer la table `opinions` en `topics` enrichis (name, summary, participants résolus, opinion, mention_count, last_seen) — couvrant mémoire thématique, recoupement social et tendances — formés la nuit et injectés au prompt.

**Architecture:** Remplacement incrémental, suite verte à chaque commit : (1) ajouter la table + helpers `topics` à côté d'`opinions`, (2) basculer la formation nocturne (`_form_opinions`→`_form_topics`) et l'injection au prompt vers `topics`, (3) retirer `opinions` une fois plus aucun appelant. La formation réutilise la passe `_form_opinions` du journal ; les participants sont résolus en `user_id` via l'alias cache ; les noms de topics existants sont injectés pour éviter la fragmentation.

**Tech Stack:** Python asyncio, aiosqlite/SQLite, DeepSeek `complete_structured`, apscheduler (via le journal), loguru.

## Global Constraints

- **Périmètre strict** : ne toucher QUE les **opinions de communauté** — la table `opinions`, `upsert_opinion`/`get_opinions`/`cleanup_opinions` (social.py), `_form_opinions` (journal.py), et les 2 blocs d'injection « Tes opinions sur les sujets de la communauté » (handlers Discord/Twitch). **NE PAS toucher** : `action_dispatcher` `note_relation` / faits `source="opinion"`, les affinités de `attention_agent`, ni le mot « opinion » dans les prompts/commentaires (`web_search`, `apex_api`, `emotion`, `vocab`, `handlers.py:79`, dashboard `about.js`).
- Logging : **loguru uniquement**. Tout async. Formation et injection **non-fatales** (`try/except`).
- LLM = `self._llm_secondary` via **`complete_structured`** (remplace `complete` + `json.loads`).
- Participants stockés en **JSON** : liste de `{"name": <pseudo>, "uid": <canonical|null>}`. Résolution via `memory._alias_cache.get(f"nickname:{pseudo.lower()}")`.
- Suite verte à CHAQUE commit (remplacement incrémental : ajouter → basculer → retirer).
- Commentaires en français, style du code environnant.

---

### Task 1 : Table `topics`

**Files:**
- Modify: `bot/db/database.py` (constante `SCHEMA`)
- Test: `tests/test_topics_db.py` (créer)

**Interfaces:**
- Produces: table `topics(id, name UNIQUE, summary, participants, opinion, mention_count, last_seen_at, created_at)`.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_topics_db.py
import pytest
from bot.db.database import Database


@pytest.mark.asyncio
async def test_topics_table_exists(tmp_path):
    db = await Database.create(str(tmp_path / "t.db"))
    rows = await db.fetch_all("PRAGMA table_info(topics)")
    cols = {r["name"] for r in rows}
    assert {"name", "summary", "participants", "opinion", "mention_count", "last_seen_at", "created_at"} <= cols
    await db.close()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m pytest tests/test_topics_db.py -q`
Expected: FAIL (table absente).

- [ ] **Step 3: Write minimal implementation**

Dans `bot/db/database.py`, dans la constante `SCHEMA` (juste après le bloc `CREATE TABLE ... opinions ...`, qui reste pour l'instant), ajouter :
```sql
CREATE TABLE IF NOT EXISTS topics (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    name          TEXT    NOT NULL UNIQUE,
    summary       TEXT,
    participants  TEXT,
    opinion       TEXT,
    mention_count INTEGER NOT NULL DEFAULT 1,
    last_seen_at  REAL    NOT NULL,
    created_at    REAL    NOT NULL
);
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m pytest tests/test_topics_db.py -q`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add bot/db/database.py tests/test_topics_db.py
git commit -m "feat(memory): table topics (sujets de communauté enrichis)"
```

---

### Task 2 : Helpers DB `topics` (ajout, sans retirer opinions)

**Files:**
- Modify: `bot/db/mixins/social.py` (classe `SocialMixin`)
- Test: `tests/test_topics_db.py` (étendre)

**Interfaces:**
- Consumes: `self._conn`, `self.execute`. `import json`, `import time` (déjà présents en tête de social.py ; sinon ajouter `import json`).
- Produces :
  - `upsert_topic(name: str, summary: str, participants: list[dict], opinion: str) -> None` — INSERT ou fusion (participants unionnés, `mention_count += 1`, `last_seen_at` = now).
  - `get_topics(limit: int = 10) -> list[dict]` — ordre `last_seen_at DESC, mention_count DESC` ; participants désérialisés.
  - `cleanup_topics(max_age_days: int = 30, max_count: int = 15) -> None`.

- [ ] **Step 1: Write the failing test** (ajouter à `tests/test_topics_db.py`)

```python
async def _mkdb(tmp_path):
    from bot.db.database import Database
    return await Database.create(str(tmp_path / "t.db"))


@pytest.mark.asyncio
async def test_upsert_topic_insert_then_merge(tmp_path):
    db = await _mkdb(tmp_path)
    await db.upsert_topic("Apex", "on en parle", [{"name": "Az", "uid": "discord:1"}], "surcoté")
    await db.upsert_topic("Apex", "encore", [{"name": "Bob", "uid": "discord:2"}, {"name": "Az", "uid": "discord:1"}], "toujours surcoté")
    topics = await db.get_topics()
    assert len(topics) == 1
    t = topics[0]
    assert t["name"] == "Apex"
    assert t["summary"] == "encore"
    assert t["opinion"] == "toujours surcoté"
    assert t["mention_count"] == 2
    uids = {p["uid"] for p in t["participants"]}
    assert uids == {"discord:1", "discord:2"}  # unionnés, pas de doublon Az
    await db.close()


@pytest.mark.asyncio
async def test_get_topics_order_and_limit(tmp_path):
    import time
    db = await _mkdb(tmp_path)
    await db.upsert_topic("Vieux", "x", [], "a")
    await db.execute("UPDATE topics SET last_seen_at = ? WHERE name='Vieux'", (time.time() - 1000,))
    await db.upsert_topic("Récent", "y", [], "b")
    topics = await db.get_topics(limit=1)
    assert [t["name"] for t in topics] == ["Récent"]
    await db.close()


@pytest.mark.asyncio
async def test_cleanup_topics_removes_old(tmp_path):
    import time
    db = await _mkdb(tmp_path)
    await db.upsert_topic("Old", "x", [], "a")
    await db.execute("UPDATE topics SET last_seen_at = ? WHERE name='Old'", (time.time() - 40 * 86400,))
    await db.cleanup_topics(max_age_days=30)
    assert await db.get_topics() == []
    await db.close()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m pytest tests/test_topics_db.py -q`
Expected: FAIL (`AttributeError: upsert_topic`).

- [ ] **Step 3: Write minimal implementation**

Dans `bot/db/mixins/social.py`, ajouter à `SocialMixin` (vérifier `import json` en tête, sinon l'ajouter) :
```python
    async def upsert_topic(
        self, name: str, summary: str, participants: list[dict], opinion: str
    ) -> None:
        """Insère un sujet ou le fusionne (participants unionnés, mention_count++)."""
        now = time.time()
        cur = await self._conn.execute(
            "SELECT id, participants FROM topics WHERE name=?", (name,)
        )
        row = await cur.fetchone()
        if row is None:
            await self.execute(
                "INSERT INTO topics (name, summary, participants, opinion, "
                "mention_count, last_seen_at, created_at) VALUES (?,?,?,?,?,?,?)",
                (name, summary, json.dumps(participants, ensure_ascii=False),
                 opinion, 1, now, now),
            )
            return
        existing = json.loads(row["participants"] or "[]")
        merged: dict[str, dict] = {}
        for p in existing + participants:
            key = p.get("uid") or p.get("name")
            if key:
                merged[key] = p
        await self.execute(
            "UPDATE topics SET summary=?, participants=?, opinion=?, "
            "mention_count=mention_count+1, last_seen_at=? WHERE id=?",
            (summary, json.dumps(list(merged.values()), ensure_ascii=False),
             opinion, now, row["id"]),
        )

    async def get_topics(self, limit: int = 10) -> list[dict]:
        """Sujets les plus chauds d'abord (récence puis fréquence)."""
        cur = await self._conn.execute(
            "SELECT name, summary, participants, opinion, mention_count, last_seen_at "
            "FROM topics ORDER BY last_seen_at DESC, mention_count DESC LIMIT ?",
            (limit,),
        )
        rows = await cur.fetchall()
        return [
            {
                "name": r["name"],
                "summary": r["summary"],
                "participants": json.loads(r["participants"] or "[]"),
                "opinion": r["opinion"],
                "mention_count": r["mention_count"],
                "last_seen_at": r["last_seen_at"],
            }
            for r in rows
        ]

    async def cleanup_topics(self, max_age_days: int = 30, max_count: int = 15) -> None:
        """Retire les sujets froids/anciens, garde les max_count plus récents."""
        cutoff = time.time() - max_age_days * 86400
        await self.execute("DELETE FROM topics WHERE last_seen_at < ?", (cutoff,))
        await self.execute(
            "DELETE FROM topics WHERE id NOT IN "
            "(SELECT id FROM topics ORDER BY last_seen_at DESC LIMIT ?)",
            (max_count,),
        )
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m pytest tests/test_topics_db.py -q`
Expected: PASS (4 tests).

- [ ] **Step 5: Run full suite**

Run: `python3 -m pytest -q`
Expected: tous verts (opinions intacts, topics ajoutés).

- [ ] **Step 6: Commit**

```bash
git add bot/db/mixins/social.py tests/test_topics_db.py
git commit -m "feat(memory): helpers DB topics (upsert fusion, get par chaleur, cleanup)"
```

---

### Task 3 : Formation nocturne `_form_topics` (remplace `_form_opinions`)

**Files:**
- Create: `bot/persona/prompts/topic_formation.md`
- Modify: `bot/intelligence/journal.py` (`_form_opinions` → `_form_topics` ; l'appel `~603`)
- Test: `tests/test_form_topics.py` (créer) ; adapter `tests/test_journal_identity.py` si le test `_form_opinions` y casse.

**Interfaces:**
- Consumes: `db.get_topics`, `db.upsert_topic`, `db.cleanup_topics` (Task 2) ; `self._llm_secondary.complete_structured(system, messages, schema, schema_name=, purpose=)` ; `self._memory._alias_cache` ; `load_prompt` de `bot.intelligence.prompts`.
- Produces: `DailyJournal._form_topics(summary_text: str)`.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_form_topics.py
import pytest
from unittest.mock import AsyncMock, MagicMock
from bot.intelligence.journal import DailyJournal


def _journal():
    config = MagicMock()
    config.bot.name = "Wally"
    j = DailyJournal(config, MagicMock(), MagicMock(), MagicMock(), MagicMock(), db=MagicMock())
    return j


@pytest.mark.asyncio
async def test_form_topics_resolves_participants_and_upserts():
    j = _journal()
    j._db.get_topics = AsyncMock(return_value=[{"name": "Apex"}])
    j._db.upsert_topic = AsyncMock()
    j._db.cleanup_topics = AsyncMock()
    j._llm_secondary.complete_structured = AsyncMock(return_value={
        "topics": [{"name": "Apex", "summary": "chaud", "participants": ["Azrael", "Inconnu"], "opinion": "surcoté"}]
    })
    # alias cache : Azrael connu, Inconnu non
    j._memory._alias_cache = {"nickname:azrael": "discord:123"}
    await j._form_topics("résumé du jour")
    # noms existants injectés au prompt (anti-fragmentation)
    payload = j._llm_secondary.complete_structured.await_args.args[1][0]["content"]
    assert "Apex" in payload
    # participants résolus : Azrael→uid, Inconnu→nom brut
    args = j._db.upsert_topic.await_args.args
    parts = args[2]
    assert {"name": "Azrael", "uid": "discord:123"} in parts
    assert {"name": "Inconnu", "uid": None} in parts


@pytest.mark.asyncio
async def test_form_topics_non_fatal_on_llm_error():
    j = _journal()
    j._db.get_topics = AsyncMock(return_value=[])
    j._db.upsert_topic = AsyncMock()
    j._db.cleanup_topics = AsyncMock()
    j._llm_secondary.complete_structured = AsyncMock(side_effect=RuntimeError("LLM down"))
    j._memory._alias_cache = {}
    await j._form_topics("résumé")  # ne lève pas
    j._db.upsert_topic.assert_not_awaited()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m pytest tests/test_form_topics.py -q`
Expected: FAIL (`AttributeError: _form_topics`).

- [ ] **Step 3: Write minimal implementation**

`bot/persona/prompts/topic_formation.md` :
```markdown
Tu es {{BOT_NAME}}. On te donne le résumé des conversations d'aujourd'hui et la liste des sujets déjà connus.

Identifie les sujets qui reviennent ou qui ont provoqué des réactions fortes (max 3). Pour chacun : son nom (RÉUTILISE un nom de la liste connue s'il correspond, n'invente pas de variante), un court résumé de ce qui s'est dit, les personnes qui en parlent (pseudos cités), et une opinion courte de {{BOT_NAME}} cohérente avec sa personnalité (aigri, sarcastique, avis tranchés). Si aucun sujet ne le mérite, retourne une liste vide.
```

Dans `bot/intelligence/journal.py` :
1. En tête (avec les autres imports), s'assurer de `from bot.intelligence.prompts import load_prompt` (sinon l'ajouter).
2. Remplacer l'appel `~603` :
```python
        if self._db is not None:
            self._fire(self._form_topics(context_text))
```
3. Remplacer entièrement la méthode `_form_opinions` par :
```python
    _TOPIC_SCHEMA = {
        "type": "object",
        "properties": {
            "topics": {
                "type": "array",
                "items": {
                    "type": "object",
                    "properties": {
                        "name": {"type": "string"},
                        "summary": {"type": "string"},
                        "participants": {"type": "array", "items": {"type": "string"}},
                        "opinion": {"type": "string"},
                    },
                    "required": ["name", "opinion"],
                },
            }
        },
        "required": ["topics"],
    }

    async def _form_topics(self, summary_text: str) -> None:
        """Analyse le résumé du jour et forme/met à jour les sujets de communauté."""
        try:
            existing = await self._db.get_topics(limit=15)
            known = ", ".join(t["name"] for t in existing) or "(aucun)"
            payload = (
                f"Sujets déjà connus (réutilise ces noms si pertinent) : {known}\n\n"
                f"Résumé du jour :\n{summary_text}"
            )
            result = await self._llm_secondary.complete_structured(
                load_prompt("topic_formation"),
                [{"role": "user", "content": payload}],
                self._TOPIC_SCHEMA,
                schema_name="topics",
                purpose="topic_formation",
            )
            for item in (result.get("topics") or [])[:3]:
                name = (item.get("name") or "").strip()
                opinion = (item.get("opinion") or "").strip()
                if not name or not opinion:
                    continue
                summary = (item.get("summary") or "").strip()
                participants = []
                for pseudo in item.get("participants") or []:
                    pseudo = (pseudo or "").strip()
                    if not pseudo:
                        continue
                    uid = self._memory._alias_cache.get(f"nickname:{pseudo.lower()}")
                    participants.append({"name": pseudo, "uid": uid})
                await self._db.upsert_topic(name, summary, participants, opinion)
                logger.info("Topic formed: {n}", n=name)
            await self._db.cleanup_topics()
        except Exception as exc:  # noqa: BLE001 — non-fatal
            logger.warning("Topic formation failed: {e}", e=exc)
```
4. Si `json` n'est plus utilisé ailleurs dans journal.py après ce remplacement, laisser l'import (ne pas chasser le dead code hors scope) — vérifier rapidement mais ne pas casser d'autres usages.

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m pytest tests/test_form_topics.py -q`
Expected: PASS (2 tests).

- [ ] **Step 5: Adapter le test d'identité orphelin**

Run: `grep -n "_form_opinions\|upsert_opinion\|opinion" tests/test_journal_identity.py`
Le test qui exerce `_form_opinions` (`test_opinions_system_prompt_uses_cindy_not_wally`) référence une méthode qui n'existe plus. Le remplacer par un test équivalent sur `_form_topics` : monter le journal Cindy, mocker `db.get_topics/upsert_topic/cleanup_topics` (AsyncMock) et `llm_secondary.complete_structured` (AsyncMock retournant `{"topics": []}`), appeler `await journal._form_topics("résumé")` directement (plus simple que via `generate_and_send`), et asserter que `complete_structured` a été appelé avec un `system_prompt` (1er arg positionnel) contenant le nom d'identité attendu (le prompt `topic_formation.md` rend `{{BOT_NAME}}`). Si l'adaptation est non triviale, la remplacer par l'assertion minimale : `complete_structured` appelé une fois.

- [ ] **Step 6: Run full suite**

Run: `python3 -m pytest -q`
Expected: tous verts (la formation écrit maintenant dans `topics` ; `get_opinions`/`upsert_opinion` ne sont plus appelés par le journal mais existent encore).

- [ ] **Step 7: Commit**

```bash
git add bot/intelligence/journal.py bot/persona/prompts/topic_formation.md tests/test_form_topics.py tests/test_journal_identity.py
git commit -m "feat(memory): _form_topics — formation nocturne enrichie (participants + anti-frag)"
```

---

### Task 4 : Injection du bloc topics (Discord + Twitch)

**Files:**
- Modify: `bot/discord/handlers.py` (bloc priorité 5, `~1257-1266`)
- Modify: `bot/twitch/handlers.py` (bloc priorité 5, `~252-261`)
- Test: adapter `tests/test_discord_handlers.py` / `tests/test_twitch_handlers.py` s'ils mockent `get_opinions`.

**Interfaces:**
- Consumes: `bot.db.get_topics(limit=5)` (Task 2).

- [ ] **Step 1: Localiser les éventuels tests à adapter**

Run: `grep -rn "get_opinions\|opinions_block\|Tes opinions sur les sujets" tests/test_discord_handlers.py tests/test_twitch_handlers.py`
Noter ce qui doit être basculé vers `get_topics` (mock) ; s'il n'y a rien, le câblage est couvert par la suite.

- [ ] **Step 2: Write the implementation**

Dans `bot/discord/handlers.py`, remplacer le bloc `# Priority 5: Community opinions` (lignes ~1257-1266) par :
```python
    # Priority 5: Community topics (sujets de communauté enrichis)
    try:
        topics = await bot.db.get_topics(limit=5)
        if topics:
            topics_block = "--- Sujets de la communauté ---"
            for t in topics:
                names = ", ".join(p["name"] for p in t["participants"]) if t["participants"] else ""
                who = f" — {names} en parlent" if names else ""
                topics_block += f'\n- {t["name"]}{who} — ton avis : "{t["opinion"]}"'
            memory_parts.append((5, topics_block))
    except Exception:
        pass
```
Dans `bot/twitch/handlers.py`, remplacer le bloc équivalent (lignes ~252-261) par le MÊME code (indentation adaptée au contexte Twitch).

Si l'étape 1 a trouvé des tests mockant `get_opinions`, les basculer pour mocker `get_topics` (retour : liste de dicts au format `get_topics`) et asserter sur « Sujets de la communauté ».

- [ ] **Step 3: Run full suite**

Run: `python3 -m pytest -q`
Expected: tous verts.

- [ ] **Step 4: Commit**

```bash
git add bot/discord/handlers.py bot/twitch/handlers.py tests/test_discord_handlers.py tests/test_twitch_handlers.py
git commit -m "feat(memory): injecte les topics enrichis au prompt (remplace le bloc opinions)"
```

---

### Task 5 : Retirer `opinions` (nettoyage final)

**Files:**
- Modify: `bot/db/mixins/social.py` (retirer `upsert_opinion`/`get_opinions`/`cleanup_opinions`)
- Modify: `bot/db/database.py` (retirer la table `opinions` du `SCHEMA` + migration `DROP TABLE IF EXISTS opinions`)
- Delete: `tests/test_evolving_opinions.py` (orphelin — testait `upsert_opinion`/`get_opinions`)

**Interfaces:**
- Consumes: rien de nouveau. Vérifie qu'aucun appelant ne subsiste.

- [ ] **Step 1: Confirmer qu'il ne reste aucun appelant**

Run: `grep -rn "get_opinions\|upsert_opinion\|cleanup_opinions\|_form_opinions" bot/ tests/`
Expected: seulement `tests/test_evolving_opinions.py` (qui sera supprimé) — aucun appel dans `bot/`.

- [ ] **Step 2: Write the implementation**

1. `bot/db/mixins/social.py` : supprimer les 3 méthodes `upsert_opinion`, `get_opinions`, `cleanup_opinions`.
2. `bot/db/database.py` : supprimer le bloc `CREATE TABLE IF NOT EXISTS opinions (...)` du `SCHEMA` ; ajouter, dans la section migrations (après `executescript(SCHEMA)`, à côté des autres `try/except aiosqlite.OperationalError`) :
```python
        # Topics remplace opinions — retire la table morte
        try:
            await conn.execute("DROP TABLE IF EXISTS opinions")
            await conn.commit()
        except aiosqlite.OperationalError:
            pass
```
3. `git rm tests/test_evolving_opinions.py`.

- [ ] **Step 3: Verify**

Run: `grep -rn "get_opinions\|upsert_opinion\|cleanup_opinions\|_form_opinions\|TABLE.*opinions\|FROM opinions\|INTO opinions" bot/ tests/`
Expected: zéro résultat.
Run: `python3 -m pytest -q`
Expected: tous verts.

- [ ] **Step 4: Commit**

```bash
git add bot/db/mixins/social.py bot/db/database.py
git rm tests/test_evolving_opinions.py
git commit -m "refactor(memory): retire opinions (remplacé par topics) + DROP table"
```

---

## Self-Review

**Spec coverage :**
- Table `topics` remplace `opinions` → Tasks 1 (ajout) + 5 (retrait). ✓
- name + summary + participants + opinion + mention_count + last_seen → Task 1 (schéma) + Task 2 (helpers). ✓
- Formation nocturne par extension de `_form_opinions` → Task 3. ✓
- Amélioration A (participants résolus via alias cache) → Task 3 (résolution `nickname:{lower}`) + Task 2 (stockage JSON {name,uid}). ✓
- Amélioration B (anti-fragmentation : noms existants injectés) → Task 3 (payload `known`). ✓
- `complete_structured` remplace `complete`+`json.loads` → Task 3. ✓
- Injection bloc topics enrichi (Discord + Twitch) → Task 4. ✓
- DROP opinions, pas de migration de données → Task 5. ✓
- Non-fatal → Tasks 3 (try/except) & 4 (try/except). ✓

**Périmètre :** le plan ne touche QUE les opinions de communauté ; `source="opinion"`/`note_relation`/affinités/prompts ne sont jamais référencés. Le grep de vérif (Task 5) cible les symboles exacts (`get_opinions`, etc.), pas le mot « opinion ».

**Suite verte à chaque commit :** Task 1 (ajout table) ✓ ; Task 2 (ajout helpers, opinions intacts) ✓ ; Task 3 (formation bascule, helpers opinions encore présents) ✓ ; Task 4 (injection bascule, plus d'appelant opinions) ✓ ; Task 5 (retrait sûr) ✓.

**Placeholder scan :** Task 3 Step 5 et Task 4 Step 1 contiennent une instruction de localisation (`grep`) + une consigne d'adaptation conditionnelle des tests existants, pas un placeholder de logique de production — le code de prod est complet. Pas d'autre placeholder.

**Type consistency :** `upsert_topic(name, summary, participants:list[dict], opinion)` / `get_topics(limit)->list[dict]` / `cleanup_topics` cohérents entre Task 2 (def), Task 3 (appels) et Task 4 (`get_topics`). Format participant `{"name","uid"}` cohérent Task 2 (merge) / Task 3 (résolution) / Task 4 (`p["name"]`). `_form_topics(summary_text)` cohérent Task 3.
